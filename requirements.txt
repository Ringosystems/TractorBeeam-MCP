mcp>=1.9.0
httpx>=0.27
python-dotenv>=1.0
uvicorn>=0.30
# Optional — only needed for the cloud-side object-lock check (cloud.py). Imported
# lazily, so the read-only core and the Graph/action/restore tiers work without it.
boto3>=1.34
